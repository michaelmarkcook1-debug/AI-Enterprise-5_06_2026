# Brand assets

The AIEnterpriseShell hero swaps between these two files based on theme:

- `ai-enterprise-light.jpg` — used in light mode
- `ai-enterprise-dark.jpg`  — used in dark mode

Drop real assets here (1600×1200 recommended, JPEG quality ~85). Until then the shell falls back to a CSS gradient so the page still builds cleanly.
